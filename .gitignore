# Dependencies
vendor/
node_modules/

# Environment & config (keep secrets safe!)
.env
config.local.php
api/config.local.php

# OS files
.DS_Store
Thumbs.db
desktop.ini

# IDE files
.vscode/
.idea/
*.sublime-project
*.sublime-workspace

# Logs & errors
*.log
error_log
php_errors.log

# Local DB dumps only — schema.sql itself IS tracked (needed for setup)
/dumps/*.sql
!schema.sql

# Compiled/cache
*.cache
*.tmp
/cache/

# Uploads (user data shouldn't be in git)
/uploads/
